# Loom — e-commerce AI assistant. Runtime dependencies.
# Flat app: install into the project venv with `pip install -r requirements.txt`
# (no editable/package install — the app runs from the repo root).
sqlalchemy>=2.0.36
psycopg[binary,pool]>=3.2.3
redis>=5.2
langgraph>=1.0
fastapi>=0.118
uvicorn[standard]>=0.34
pyyaml>=6.0.3
langchain>=1.3.11
langchain-anthropic>=1.4.7
langchain-openai>=1.3.3
# Storefront layer (auth + semantic retrieval)
pyjwt>=2.13
qdrant-client>=1.18
