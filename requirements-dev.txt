# Loom — development / test dependencies.
# Install with `pip install -r requirements-dev.txt` (pulls in runtime deps too).
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.28
# Pinned exactly: ruff's default rule set widens between releases, so a floor pin
# (>=) lets CI enforce rules the code was never written against. Bump deliberately.
ruff==0.15.20
